<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agriapp_t">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.nutrient_input.SoilDetailsActivity"></activity>
        <activity android:name=".ui.crop_details.CurrentCropActivity" />
        <activity android:name=".ui.land_dashboard.LandDashboardActivity" />
        <activity android:name=".ui.land_list.AddLandActivity" />
        <activity android:name=".ui.land_list.LandListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".AddNoteActivity"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>